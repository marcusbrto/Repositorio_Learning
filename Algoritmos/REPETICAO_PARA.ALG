algoritmo "REPETICAO_PARA"
var
    i,tabuada,soma : inteiro
inicio

Escreval("Iremos realizar a tabuada de qual número? ")
leia(tabuada)
      Para i de 1 até 10 faça
      soma <- tabuada * i
           Escreval(tabuada," X",i," = ",soma)
      fimpara
      
fimalgoritmo